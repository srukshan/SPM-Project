package com.Controller.Complexity.TypeOf;

import static org.junit.Assert.*;

import org.junit.Test;

import com.Interface.ComplexityFinder;
import com.Model.Complexity;

public class IfConditionComplexityTest {

	@Test
	public void test() {
		/*
		 * ComplexityFinder complexityFinder = new
		 * IfConditionComplexity("   if(a+b==\"hello\"&&d-c==2) {"); Complexity
		 * complexity = complexityFinder.GetComplexity(); assertEquals("&&, if",
		 * complexity.keywordsToString()); assertEquals(2, complexity.getScore());
		 */
	}

}
